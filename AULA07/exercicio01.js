let listas = document.getElementsByTagName('li');

// Altera a cor de fundo de cada item da lista
for (let i = 0; i < listas.length; i++) {
    listas[i].style.backgroundColor = 'lightblue'; 
};

let elemento01 = window.document.getElementById('tituloPrin')
elemento01.innerHTML = elemento02

let elemento02 = window.document.getElementById('listas')
elemento02.innerHTML = elemento01

// Altera a cor de fundo de cada parágrafo
// Seleciona todos os elementos <p> no documento