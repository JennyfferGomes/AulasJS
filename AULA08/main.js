let lista = window.document.querySelectorAll('li');
lista[0].style.color = 'red';

for (let num = 0; num < lista.length; num++) { // declarar valor da variável | teste de lógico | incremento
lista[num].style.color = 'red';
}